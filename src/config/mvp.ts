export const MVP_MODE = true;

// Tabs visible in MVP — order matters (sidebar renders in this order)
export const MVP_NAV = ['home', 'log', 'tasks', 'memory', 'mentor', 'community', 'habits', 'settings'] as const;
